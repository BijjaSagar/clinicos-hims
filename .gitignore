# ── This remote tracks only `backend/` (Laravel). Ignore everything else in the repo root. ──
/*
!/backend/
!/backend/**
!/README.md
!/.gitignore

# (Optional) If you add stray files at repo root later, they stay untracked thanks to `/*` above.
